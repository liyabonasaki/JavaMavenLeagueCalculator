# JavaMavenLeagueCalculator

#RUNNING THE APPLICATION ON CLI
  
  -install maven first
  -install java 8 or later
  -clone the repo -> git clone 
  navigate to the target folder
  mvn package install;
  cd target;
  java -cp myApp-0.0.1-SNAPSHOT.jar mypackage.MyClass
